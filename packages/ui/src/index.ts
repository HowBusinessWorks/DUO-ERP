/**
 * Design system si shell-ul de navigare.
 *
 * Gol intentionat in pasul 01. Pasul 03 aduce: shell-ul recursiv (sidebar +
 * breadcrumb dublu + tab-uri per entitate), pagina fractala si componentele
 * de baza. Pasul 01 nu construieste niciun ecran.
 */

export const UI_PACKAGE_READY = true;
