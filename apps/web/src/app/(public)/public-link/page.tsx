export default function PublicLinkPage() {
  return (
    <main>
      <h1>Acces prin link</h1>
      <p>
        Route group <code>(public)</code> — pagini accesibile fără cont, prin link tokenizat.
      </p>
    </main>
  );
}
